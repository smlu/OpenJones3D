layout(std140) uniform CameraData {
    mat4 VIEW;
    mat4 INVERSE_VIEW;
    mat4 PROJECTION;
    mat4 INVERSE_PROJECTION;
    mat4 VIEWPROJECTION;
    float TIME;
};

layout(std140) uniform ViewportData {
    vec4 VIEWPORT;
};
